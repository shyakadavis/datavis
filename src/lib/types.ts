export type StudentData = {
	name: string;
	hours: number;
	grade: number;
};

export type Margin = {
	top: number;
	right: number;
	bottom: number;
	left: number;
};

export type TicksParams = {
	interval: number;
	array_length: number;
};
